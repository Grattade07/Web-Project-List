{
  "name": "t53---restful-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix web-project-interface && npm run build --prefix web-project-interface"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "express": "^4.18.2",
    "react-redux": "^8.0.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
